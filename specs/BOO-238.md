# BOO-238 — docs: Demo-Runbook + vorgefertigtes Demo-Projekt (Kunde 1–2 h)

## Summary

Zwei Runbooks, um das Framework beim Kunden live in 1–2 h vorzuführen: das **Ablauf-Drehbuch** (`customer-demo.md`, *während* der Demo) + das **Vorbereitungs-Runbook** (`demo-vorbereitung.md`, *davor*). Reine Doku. Sprint 8.

## Why

Operator-Wunsch (2026-06-20): das Framework beim Kunden in 1–2 h zeigen können — „so wird entwickelt", aus verschiedenen Rollen. Braucht ein step-by-step Drehbuch und ein vorgefertigtes Demo-Projekt.

## What

- Neues Runbook `docs/runbooks/customer-demo.md` (+ `.en.md`): Vorbereitungs-Checkliste (Accounts/Keys, Trockenlauf, Offline-Fallback), 1–2-h-Ablauf mit Timing + Talking Points, Rollen-Use-Cases (CISO/CTO/DPO/CEO/Auditor) mit Verweis auf die Rollen-Runbooks, Gate-Block-live-Sequenz, Reset/Idempotenz, „was bewusst NICHT live passiert" (Merge/Deploy/Secrets, Verweis „Unsere Position").
- `docs/INDEX.md` + `.en.md`: neue Zeile (DE+EN).
- Outbound-Links auf Rollen-Runbooks, Onboarding-Checklisten, Cheat-Sheet, „Unsere Position".
- **Teil 2 — `docs/runbooks/demo-vorbereitung.md` (+ `.en.md`):** Vorbereitungs-Drehbuch (VPS → `/bootstrap` → Beispiel-Story `POST /login` Node/TS + kopierfertiger Prompt → Feature-Walkthrough → Reset). In README („Where to Start"/„Wo anfangen?") verlinkt, INDEX DE+EN, mit `customer-demo.md` cross-verlinkt. Bewusst **kein gepflegtes Demo-Repo** (Operator-Entscheid 2026-06-21).

## Constraints

- Reine Doku, kein Verhaltens-Change.
- Gegen reale Framework-Mechanik gegroundet (Gates, Skills, Rollen-Runbooks, `/sprint-run --auto`), keine erfundenen Schritte.
- DE/EN paritätisch; `docs_drift_check.py` rc=0.

## Acceptance Criteria

1. [x] Demo-Runbook DE+EN — 1–2-h-Drehbuch mit Timing + Talking Points
2. [x] Rollen-Use-Cases (CISO/CTO + weitere) als Demo-Szenen
3. [x] Live-Ablauf Gate-Block → Auflösung + `/sprint-run --auto`
4. [x] Vorbereitungs-Checkliste + Reset-Prozedur
5. [x] **Reproduzierbare Demo-Vorbereitung als Runbook** (statt gepflegtem Repo): `demo-vorbereitung.md` (+ `.en.md`) — VPS → `/bootstrap` → Beispiel-Story (`POST /login`, Node/TS) + kopierfertiger Prompt → Feature-Walkthrough → Reset; in README verlinkt
6. [x] In INDEX DE+EN registriert, docs-drift grün

## Result

Beide Teile umgesetzt: Ablauf-Drehbuch (Sprint 8, PR #107) + Vorbereitungs-Runbook (Branch `feat/boo-238-demo-vorbereitung`). Operator-Entscheid 2026-06-21: Vorbereitungs-Runbook statt gepflegtem Demo-Repo — alle ACs erfüllt.
