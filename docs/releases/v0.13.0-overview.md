# v0.13.0 — Sprint 6: Status Line + Validierung + Restklärung (BOO-205–217)

**Highlight:** Sprint 6 macht das in Sprint 4 eingeführte **Worker-Equivalent live** — Modell, Token-Budget, Story-Budget und Menschen-Äquivalent (h/h) stehen jetzt nativ in der Claude-Code-**Status Line** (Schalter-A-gekoppelt). Dazu schreibt der **`backlog`-Skill** erstmals zurück (geführter, dry-run-default Sprint-Plan-Sync nach Linear), **ADR-5 (Hermes)** wird festgezurrt und um einen **monatlichen Native-Feature-Wächter** ergänzt, und die **Infra-Layer-Verankerung** ist geklärt (13-Layer-Raster → Folge-Stories BOO-220/221/223). Additiv — keine Breaking Changes.

## Sprint-Sammelnote

Vollständige Sprint-Note (Highlights, Migration, Schalter, Doku-Updates, Stories-Mapping, ADR-Verweise): **[`docs/releases/sprint-6.md`](sprint-6.md)** (DE + EN in einer Datei).

## Was in diesem Release steckt (Sprint 6)

| BOO | Thema |
|-----|-------|
| **BOO-205** | Status-Line-Custom-Script mit Worker-Equivalent live (`statusline.sh`, `migrate_boo_205`, verify §5c, HANDBUCH Anhang AQ). bootstrap 3.44.0. |
| **BOO-194** | `backlog` Schritt 6 Sprint-Plan-Sync — erster schreibender Modus (Label `sprint-N`, Dry-Run-Default, Audit-Log, `/backlog sync`). backlog 1.8.0. |
| **BOO-202** | ADR-5 Hermes-Layer vs Agent Teams `entschieden` + monatliche Native-Feature-Beobachtung. quality-gate-audit 1.1.0. |
| **BOO-217** | Infra-Layer-Verankerung geklärt (Ideation) → Folge-Stories BOO-220/221/222/223 (+ BOO-224 geparkt). |
| **BOO-215** | Finaler Doku-Smoke-Test Sprint 3–6 (4 Personas) + v1.0-Major-Doku (`v1.0-native-pivot.md`). |

> **Operator-async:** BOO-218 (`/goal`-E2E-Lauf) läuft beim Framework-Reinstall und schließt die zwei offenen BOO-203-Checkboxen — blockiert dieses Release nicht.

## Breaking Changes (Kurzfassung)

Keine. Die Status Line greift nur bei `runtime_target=claude-code` (Schalter A); der Backlog-Sync ist opt-in/dry-run-default; die Native-Feature-Beobachtung ist eine Meldung, kein Gate. Bestandsprojekte ziehen idempotent via `intentron migrate` (`migrate_boo_205`) nach.

## Schalter

**Status Line** — an `runtime_target=claude-code` gekoppelt; Render aus `settings.json`, Manifest-Referenz in `environment.json`. **Backlog-Sprint-Sync** — kein Default, nur `/backlog sync`, Dry-Run zuerst.

## ADR-Verweise

- **ADR-5 — Hermes-Layer vs Agent Teams** (Status quo halten; Re-Eval-Trigger 1 = Agent Teams *stable*). BOO-202.

## Ausblick v1.0

`docs/releases/v1.0-native-pivot.md` (+ `.en.md`) fasst die Sprint-3-bis-6-Transformation (Build-vs-Buy, Schalter A/B/C, `/goal`-Engine, native Subagents, Status Line) als Major-/Pitch-Dokument zusammen. **Tag/Rollout v1.0.0** = post-Sprint-6 / BOO-9.

## Verifikation

`docs-drift-check` grün (0 Befunde) · DE/EN-Parität · SKILL↔README-Gleichstand · CI grün (PRs #95, #96, #97).

---

# 🇬🇧 English Version

# v0.13.0 — Sprint 6: Status line + validation + remaining clarifications (BOO-205–217)

**Highlight:** Sprint 6 makes the **worker-equivalent** introduced in Sprint 4 **live** — model, token budget, story budget and human equivalent (h/h) now sit natively in the Claude Code **status line** (switch-A coupled). On top, the **`backlog` skill** writes back for the first time (guided, dry-run-default sprint-plan sync to Linear), **ADR-5 (Hermes)** is locked and extended with a **monthly native-feature watcher**, and the **infra-layer anchoring** is clarified (13-layer grid → follow-up stories BOO-220/221/223). Additive — no breaking changes.

## Sprint roll-up note

Full sprint note (highlights, migration, switches, doc updates, stories mapping, ADR references): **[`docs/releases/sprint-6.md`](sprint-6.md)** (DE + EN in one file).

## What's in this release (Sprint 6)

| BOO | Topic |
|-----|-------|
| **BOO-205** | Status-line custom script with worker-equivalent live (`statusline.sh`, `migrate_boo_205`, verify §5c, HANDBUCH Appendix AQ). bootstrap 3.44.0. |
| **BOO-194** | `backlog` step 6 sprint-plan sync — first writing mode (label `sprint-N`, dry-run default, audit log, `/backlog sync`). backlog 1.8.0. |
| **BOO-202** | ADR-5 Hermes layer vs Agent Teams `decided` + monthly native-feature watch. quality-gate-audit 1.1.0. |
| **BOO-217** | Infra-layer anchoring clarified (ideation) → follow-up stories BOO-220/221/222/223 (+ BOO-224 parked). |
| **BOO-215** | Final doc smoke test Sprint 3–6 (4 personas) + v1.0 major doc (`v1.0-native-pivot.md`). |

> **Operator-async:** BOO-218 (`/goal` E2E run) runs at framework reinstall and closes the two open BOO-203 checkboxes — it does not block this release.

## Breaking changes (short form)

None. The status line only fires under `runtime_target=claude-code` (switch A); the backlog sync is opt-in/dry-run-default; the native-feature watch is a report, not a gate. Existing projects pull it idempotently via `intentron migrate` (`migrate_boo_205`).

## Switches

**Status line** — coupled to `runtime_target=claude-code`; rendered from `settings.json`, manifest reference in `environment.json`. **Backlog sprint sync** — no default, `/backlog sync` only, dry-run first.

## ADR references

- **ADR-5 — Hermes layer vs Agent Teams** (hold the status quo; re-eval trigger 1 = Agent Teams *stable*). BOO-202.

## Outlook v1.0

`docs/releases/v1.0-native-pivot.md` (+ `.en.md`) sums up the Sprint-3-to-6 transformation (build-vs-buy, switches A/B/C, `/goal` engine, native subagents, status line) as a major/pitch document. **Tag/rollout v1.0.0** = post-Sprint-6 / BOO-9.

## Verification

`docs-drift-check` green (0 findings) · DE/EN parity · SKILL↔README lockstep · CI green (PRs #95, #96, #97).
</content>
