# v0.12.0 — Sprint 5: Claude-Code-first, Schalter A/B/C + Implement-Migration (BOO-199–213)

**Highlight:** Sprint 5 vollzieht den **Claude-Code-first-Pivot** (ADR-2). Drei Schalter aktivieren native Claude-Code-Pfade, statt sie nachzubauen: **A** setzt `runtime_target=claude-code` als Default + einen `native_paths`-Block in der CLAUDE.md; **B** schaltet echte native Subagents (`/implement`), den `/ultraplan`-Trigger (`/architecture-review` + `/ideation`) und den neuen `insights-review`-Skill scharf; **C** liefert Routinen als Runbook statt Bootstrap-Frage. Dazu: ADR-3 (Memory-Abgrenzung), die 13-Komponenten-USP-Liste und `/rewind`-Doku. Additiv — keine Breaking Changes.

## Sprint-Sammelnote

Vollständige Sprint-Note (Highlights, Migration, Schalter, Doku-Updates, Stories-Mapping, ADR-Verweise): **[`docs/releases/sprint-5.md`](sprint-5.md)** (DE + EN in einer Datei).

## Was in diesem Release steckt (9 Stories)

| BOO | Thema |
|-----|-------|
| **BOO-199** | Schalter A — `runtime_target=claude-code`-Default + `native_paths`-Block (ADR-2). bootstrap 3.41.0. |
| **BOO-200** | ADR-3 Automemory vs Learning-Loop + Bootstrap-MEMORY.md-Header (HANDBUCH Anhang AM). bootstrap 3.42.0. |
| **BOO-204** | `/implement` → native Subagents mit eigenem 200k-Window (`## Subagents`-Konvention). implement 2.16.0. |
| **BOO-208** | `/rewind`-Doku in `/implement` + HANDBUCH §11b „Checkpoints". |
| **BOO-207** | `/architecture-review` + `/ideation` Ultraplan-Trigger (HANDBUCH Anhang AN, `## Plan`-Konvention). |
| **BOO-206** | neuer Skill `insights-review` (1.0.0) + `/sprint-review` Schritt 7d (`complement_insights`). |
| **BOO-211** | HANDBUCH Anhang AO „Sprint Review vs /insights". |
| **BOO-213** | Schalter C — Runbook `routinen-vernetzen.md` (3 Aufstellungen, 7 Routinen). bootstrap 3.43.0. |
| **BOO-209** | USP-Liste „13 Komponenten ohne Anthropic-Pendant" (HANDBUCH Anhang AP, pitch 1.2.0). |

## Breaking Changes (Kurzfassung)

Keine. Der `runtime_target`-Default-Wechsel betrifft nur Neuanlagen; Bestandsprojekte ziehen idempotent via `intentron migrate` (`migrate_boo_199`/`migrate_boo_200`) nach. Explizite `codex`/`cross-tool`-Wahl bleibt unangetastet.

## Schalter

`native_paths`-Block (nur bei `runtime_target: claude-code`): `prefer_native_subagents` (true), `prefer_goal_termination` (true), `prefer_ultraplan` (auto), `complement_insights` (true).

## ADR-Verweise

- **ADR-2 — Claude-Code-first Pivot, Schalter A B C** (Anhang AL/AN/AO, BOO-199).
- **ADR-3 — Automemory vs Learning-Loop** (Anhang AM, BOO-200).
- **ADR-1 — Build-vs-Buy-Doktrin** (Anhang AG/AP, BOO-198/209).

## Verifikation

`docs-drift-check` grün (0 Befunde, 20 Skills) · DE/EN-Parität · SKILL↔README-Gleichstand · CI grün (PRs #88, #89, #91, #92, #93).

---

# 🇬🇧 English Version

# v0.12.0 — Sprint 5: Claude-Code-first, switches A/B/C + implement migration (BOO-199–213)

**Highlight:** Sprint 5 performs the **Claude-Code-first pivot** (ADR-2). Three switches activate native Claude Code paths instead of rebuilding them: **A** sets `runtime_target=claude-code` as the default + a `native_paths` block in CLAUDE.md; **B** arms real native subagents (`/implement`), the `/ultraplan` trigger (`/architecture-review` + `/ideation`) and the new `insights-review` skill; **C** ships routines as a runbook instead of a bootstrap question. Plus: ADR-3 (memory boundary), the 13-component USP list and `/rewind` docs. Additive — no breaking changes.

## Sprint roll-up note

Full sprint note (highlights, migration, switches, doc updates, stories mapping, ADR references): **[`docs/releases/sprint-5.md`](sprint-5.md)** (DE + EN in one file).

## What's in this release (9 stories)

| BOO | Topic |
|-----|-------|
| **BOO-199** | switch A — `runtime_target=claude-code` default + `native_paths` block (ADR-2). bootstrap 3.41.0. |
| **BOO-200** | ADR-3 Automemory vs Learning-Loop + bootstrap MEMORY.md header (HANDBUCH Appendix AM). bootstrap 3.42.0. |
| **BOO-204** | `/implement` → native subagents with own 200k window (`## Subagents` convention). implement 2.16.0. |
| **BOO-208** | `/rewind` docs in `/implement` + HANDBUCH §11b "checkpoints". |
| **BOO-207** | `/architecture-review` + `/ideation` ultraplan trigger (HANDBUCH Appendix AN, `## Plan` convention). |
| **BOO-206** | new skill `insights-review` (1.0.0) + `/sprint-review` step 7d (`complement_insights`). |
| **BOO-211** | HANDBUCH Appendix AO "Sprint Review vs /insights". |
| **BOO-213** | switch C — runbook `routinen-vernetzen` (3 setups, 7 routines). bootstrap 3.43.0. |
| **BOO-209** | USP list "13 components without an Anthropic equivalent" (HANDBUCH Appendix AP, pitch 1.2.0). |

## Breaking changes (short form)

None. The `runtime_target` default change affects new projects only; existing projects pull it idempotently via `intentron migrate` (`migrate_boo_199`/`migrate_boo_200`). An explicit `codex`/`cross-tool` choice stays untouched.

## Switches

`native_paths` block (only when `runtime_target: claude-code`): `prefer_native_subagents` (true), `prefer_goal_termination` (true), `prefer_ultraplan` (auto), `complement_insights` (true).

## ADR references

- **ADR-2 — Claude-Code-first pivot, switches A B C** (Appendix AL/AN/AO, BOO-199).
- **ADR-3 — Automemory vs Learning-Loop** (Appendix AM, BOO-200).
- **ADR-1 — build-vs-buy doctrine** (Appendix AG/AP, BOO-198/209).

## Verification

`docs-drift-check` green (0 findings, 20 skills) · DE/EN parity · SKILL↔README lockstep · CI green (PRs #88, #89, #91, #92, #93).
