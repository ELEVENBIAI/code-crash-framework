# v0.11.0 — Sprint 3: Build-vs-Buy-Pivot, /sprint-run auf `/goal` (BOO-198–216)

**Highlight:** Sprint 3 vollzieht den **Build-vs-Buy-Pivot**: `/sprint-run` baut die Sprint-Termination-Schleife nicht mehr selbst, sondern fährt sie auf der **Anthropic-nativen `/goal`-Engine**. Der Skill schrumpft auf **v2.0.0** (Konfigurator + `/goal`-Wrapper); das Hybrid-Container-Daemon-Modell ist **superseded** (ADR-4). Leitsatz: Framework = Governance-Skelett, Anthropic = Motor. Dazu: neue `/goal`-Doku (`goal/`), Build-vs-Buy-Doktrin (ADR-1), Doku-Konsistenz- und Release-Notes-Standards.

## Sprint-Sammelnote

Vollständige Sprint-Note mit Highlights, Breaking Changes, Migrations-Anleitung, Schaltern, Doku-Updates, Stories-Mapping und ADR-Verweisen: **[`docs/releases/sprint-3.md`](sprint-3.md)** (+ [`.en.md`](sprint-3.en.md)).

## Was in diesem Release steckt (7 Stories)

| BOO | Thema |
|-----|-------|
| **BOO-198** | ADR-1 Build-vs-Buy-Doktrin (HANDBUCH Anhang AG) — das Framework baut nicht nach, was Anthropic nativ liefert. |
| **BOO-201** | ADR-4 — `/goal` löst das Hybrid-Container-Daemon-Modell ab (HANDBUCH Anhang AD/AH). |
| **BOO-203** | `/sprint-run`-Refactor auf Konfigurator + `/goal` — Skill **v2.0.0**. |
| **BOO-210** | `/goal`-Doku + `goal/README` (HANDBUCH Anhang AH) + Phrasen-/E2E-Referenzen. |
| **BOO-212** | Cleanup alte Hybrid-Doku — Daemon-Loop-Runbook *veraltet seit v0.11.0*. |
| **BOO-214** | Doku-Konsistenz-Standard + Story-Template-Pflichtblock (HANDBUCH Anhang AI). |
| **BOO-216** | Release-Notes-Standard: Sprint-/Major-Note + Index (HANDBUCH Anhang AJ). |

## Breaking Changes (Kurzfassung)

- **`/sprint-run` v1.x → v2.0.0:** Eigenbau-Daemon-Loop entfällt, Ausführung läuft über `/goal` mit Termination-Phrase.
- **Container-Logik entfällt:** Hybrid-Container-Modell superseded → drei `/goal`-Boundaries (Worktree-Isolation, Bash-Allowlist, Layer-0-Bodyguard).
- **Altes `/sprint-run`-Runbook** ist nur noch historische Referenz.

Migration für Bestandsprojekte: `.claude/settings.local.json` Bash-Allowlist anlegen, `execution_isolation=worktree`, Bodyguard aktiv — Details in [`sprint-3.md`](sprint-3.md).

## ADR-Verweise

- **ADR-1 — Build-vs-Buy-Doktrin** (Anhang AG, BOO-198).
- **ADR-4 — Sprint-Run mit `/goal`** (Anhang AD/AH, BOO-201/203).

## Verifikation

`docs-drift-check` grün (0 Befunde) · DE/EN-Parität · `sprint-run` SKILL↔README v2.0.0.

---

# 🇬🇧 English Version

# v0.11.0 — Sprint 3: build-vs-buy pivot, /sprint-run on `/goal` (BOO-198–216)

**Highlight:** Sprint 3 performs the **build-vs-buy pivot**: `/sprint-run` no longer builds the sprint termination loop itself but runs it on the **Anthropic-native `/goal` engine**. The skill shrinks to **v2.0.0** (configurator + `/goal` wrapper); the hybrid-container daemon model is **superseded** (ADR-4). Guiding principle: framework = governance skeleton, Anthropic = engine. Plus: new `/goal` docs (`goal/`), build-vs-buy doctrine (ADR-1), documentation-consistency and release-notes standards.

## Sprint roll-up note

Full sprint note with highlights, breaking changes, migration guide, switches, documentation updates, stories mapping and ADR references: **[`docs/releases/sprint-3.en.md`](sprint-3.en.md)** (+ [`.md`](sprint-3.md)).

## What's in this release (7 stories)

| BOO | Topic |
|-----|-------|
| **BOO-198** | ADR-1 build-vs-buy doctrine (HANDBUCH Appendix AG) — the framework does not rebuild what Anthropic delivers natively. |
| **BOO-201** | ADR-4 — `/goal` supersedes the hybrid-container daemon model (HANDBUCH Appendix AD/AH). |
| **BOO-203** | `/sprint-run` refactor to configurator + `/goal` — skill **v2.0.0**. |
| **BOO-210** | `/goal` docs + `goal/README` (HANDBUCH Appendix AH) + phrase/E2E references. |
| **BOO-212** | cleanup of old hybrid docs — daemon-loop runbook *deprecated since v0.11.0*. |
| **BOO-214** | documentation-consistency standard + story-template mandatory block (HANDBUCH Appendix AI). |
| **BOO-216** | release-notes standard: sprint/major note + index (HANDBUCH Appendix AJ). |

## Breaking changes (short form)

- **`/sprint-run` v1.x → v2.0.0:** home-grown daemon loop removed, execution runs through `/goal` with a termination phrase.
- **Container logic removed:** hybrid-container model superseded → three `/goal` boundaries (worktree isolation, bash allowlist, layer-0 bodyguard).
- **Old `/sprint-run` runbook** is a historical reference only.

Migration for existing projects: create the `.claude/settings.local.json` bash allowlist, set `execution_isolation=worktree`, keep the bodyguard active — details in [`sprint-3.en.md`](sprint-3.en.md).

## ADR references

- **ADR-1 — build-vs-buy doctrine** (Appendix AG, BOO-198).
- **ADR-4 — sprint-run with `/goal`** (Appendix AD/AH, BOO-201/203).

## Verification

`docs-drift-check` green (0 findings) · DE/EN parity · `sprint-run` SKILL↔README v2.0.0.
